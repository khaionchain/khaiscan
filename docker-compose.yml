version: "3.9"

services:
  khaiscan:
    build: .
    container_name: khaiscan-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
